# Minecraft but Techies was here

* Requirements
  
Need to have Fabric Loader and Fabric API installed.
* Installation

Just drop it in the /mods folder. A config file will be generated in /config folder to tweak some settings if needed. 